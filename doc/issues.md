# Issues (high-value next steps, unresolved)

## 1) インボックス最小実装（メモ取り込み）
- 背景: 手入力のみで、要件の「複数インボックスから自動取込」が未着手。導入障壁が高い。
- スコープ: `/api/intake/memo` でメモをPOSTするとバックログにタスク生成し、重複判定（タイトル類似）を返す。UIに「メモ貼り付け→分析中→重複候補〇/×」の最小フローを追加。
- 受入れ: 1) API経由でタスク作成 2) 類似タスク候補がレスポンスに含まれる 3) UIで候補をマージor新規作成を選択できる。

## 2) AI提案を「適用」できるアクション化
- 背景: AI提案ログは閲覧のみで、タスクへの反映は手作業。AI価値を定着させにくい。
- スコープ: バックログ/設定の提案カードに「適用」ボタンを付け、分解・スコア更新・メモ追記をワンクリックで反映。適用履歴をAIログに紐付け。
- 受入れ: 1) 提案カードに適用ボタン 2) 押下でタスクが更新/分解される 3) 適用がログに記録される。

## 3) ダッシュボードを実データのみで表示（空データCTA強化）
- 背景: ベロシティ/バーンダウンがダミー系列を表示し、実績との乖離で信用を損なう。
- スコープ: フォールバック系列を撤廃し、データゼロ時はCTA（スプリント開始/タスク追加/インテグレーション設定）を表示。実データから平均/最高値を計算。
- 受入れ: 1) データがない時にグラフは空＋明確なCTA 2) 値はDBの実データのみで算出 3) 見た目の崩れがない。

## 3) キャパ/依存のガードレール
- 背景: スプリント移動でキャパ超過・依存未完でも通過でき、計画精度が落ちる。
- スコープ: バックログ→スプリント移動時に残容量チェックと依存未完警告を行う。超過時はエラー、依存未完は確認ダイアログ。ダッシュボードに「キャパ使用率」と「未解決依存数」を表示。
- 受入れ: 1) キャパ超過移動が阻止される 2) 未完依存は警告付きで操作 3) ダッシュボードにメトリクスが反映される。

## 4) Plan First ナビ/初期遷移の再設計
- AS-IS: ナビ順はダッシュボード中心で、初見の導線がPlanに寄っていない。
- TO-BE: 初回/再訪の入口をPlanへ寄せ、Plan→Execute→Reviewの順に揃える。
- スコープ: 初期遷移を Plan（/backlog or /sprint）へ寄せ、ナビ順も Plan→Execute→Review に並べ替える。
- 受入れ: 1) 初回/再訪で Plan へ誘導 2) ナビが Plan中心の順序 3) 既存導線が破綻しない。

## 5) オンボーディング v2（認知オフロード重視）
- AS-IS: 4ステップだが、アジャイルの流れとAIの役割が明示されない。
- TO-BE: 目的→目標/ルーティン→やるべきこと3件→AI下準備体験の一本道にする。
- スコープ: 目的→目標/ルーティン→やるべきこと3件→AI下準備プレビューの一本道に再設計。
- 受入れ: 1) 最初の目標/ルーティンが作成される 2) やるべきこと3件が表示 3) AI下準備の例が体験できる。

## 6) やるべきこと（3件）生成ロジック
- AS-IS: 集中対象の提示がなく、バックログ/スプリントから自力で選ぶ。
- TO-BE: スコア+期限の正規化で3件に絞り、Planの入口に固定表示。
- スコープ: スコア+期限正規化（0.7/0.3）で3件を算出し、ダッシュボードに固定表示。
- 受入れ: 1) 3件だけ表示 2) 期限/スコアに応じて並ぶ 3) 数値スコアは非表示。

## 7) やるべきことの保存と履歴
- AS-IS: やるべきことは存在せず、履歴も無い。
- TO-BE: 選定理由と結果を保存し、後から改善できるようにする。
- スコープ: FocusQueue（仮）として選定理由/算出日時を保存。
- 受入れ: 1) 選定結果が保存される 2) 選定理由が記録される 3) 直近履歴が参照できる。

## 8) AI下準備モーダル（タスク詳細）
- AS-IS: AI提案は点在しており、タスク詳細から実行しづらい。
- TO-BE: タスク詳細のモーダルで下準備を完結させる。
- スコープ: タスク詳細から「AIで下準備」→アウトプットをモーダル表示。
- 受入れ: 1) タスクからモーダルを開ける 2) 下準備が保存される 3) 閉じても再閲覧可能。

## 9) AI下準備のアウトプット種別
- AS-IS: アウトプットの型が統一されておらず再利用しづらい。
- TO-BE: まずは3種に絞って認知負荷を下げる。
- スコープ: まずは「メール文」「実装方針」「チェックリスト」の3種に限定。
- 受入れ: 1) 種別選択できる 2) それぞれの出力が得られる 3) ログに残る。

## 10) 低リスク基準の段階的引き上げ
- AS-IS: 低リスク判定は固定しきい値で、個人差に追従できない。
- TO-BE: 保守的な初期値から段階的に引き上げる。
- スコープ: 初期は保守的なスコア閾値、利用状況に応じて段階引き上げ。
- 受入れ: 1) 初期閾値が設定できる 2) 段階が記録される 3) いつでも戻せる。

## 11) AI下準備の承認フロー統一
- AS-IS: AI出力の適用が画面ごとに分散している。
- TO-BE: どのAI出力でも同じ承認/適用UIに統一する。
- スコープ: “承認→適用” の共通UIを用意し、どの出力でも同じ操作に統一。
- 受入れ: 1) 承認操作が統一UI 2) 監査ログに残る 3) 取り消し可能。

## 12) Readiness/DoD（完了条件）管理
- AS-IS: 完了条件が曖昧で、タスクが滞留しやすい。
- TO-BE: 完了条件とチェックリストで“終わらせ方”を明確にする。
- スコープ: WorkItemに完了条件とチェックリストを持たせる。
- 受入れ: 1) 完了条件を入力できる 2) チェックが進捗として見える 3) DONEに移す条件が明確になる。

## 13) ルーティンの再発生ルール
- AS-IS: ルーティンが単発になり、継続性が落ちる。
- TO-BE: 繰り返しルールで再発生させる。
- スコープ: ルーティンの繰り返しルール（毎日/毎週）を設定可能にする。
- 受入れ: 1) ルールを設定できる 2) 自動で再生成される 3) 未完了数に含まれる。

## 14) PBIベロシティの明示
- AS-IS: Sprint速度のみが目立ち、PBI消化が見えにくい。
- TO-BE: PBI消化率と速度をレビューに含める。
- スコープ: PBI消化率とPBI速度をレビューで表示。
- 受入れ: 1) PBI完了数が可視化 2) Sprint速度と並べて見える 3) 期間比較が可能。

## 15) 持続ベロシティ指標（ブレの可視化）
- AS-IS: 単発の速度指標が中心で、安定性が見えにくい。
- TO-BE: 直近Nスプリントの平均と分散を表示して持続性を見る。
- スコープ: 直近Nスプリントの平均・分散を表示。
- 受入れ: 1) 平均/分散が見える 2) 持続可能なレンジが提示される 3) 体感と一致する。

## 16) User/Workspace Memory 基盤
- AS-IS: ユーザー前提を保存する仕組みがなく、学習が積み上がらない。
- TO-BE: Memoryを明示的に保存し、編集/削除できる。
- スコープ: UserMemory/WorkspaceMemory を保存し、編集/削除UIを用意。
- 受入れ: 1) 最小セットが保存される 2) ユーザーが編集できる 3) 参照元が明示される。

## 17) Memory 確認モーダル（confidence>=0.7）
- AS-IS: 推定情報の確認手段がない。
- TO-BE: 一定の確信度で確認モーダルを出し、確定させる。
- スコープ: 確信度0.7以上で質問モーダルを表示し、肯定/否定/保留を記録。
- 受入れ: 1) モーダルが出る 2) 結果が保存される 3) 後から変更可能。

## 18) Plan/Execute/Review の接続強化
- AS-IS: Plan/Execute/Review が分断され、意図が流れない。
- TO-BE: PlanからSprint/Reviewへ意図がつながる導線を作る。
- スコープ: Planで決めた内容がSprint/Reviewに反映される導線を追加。
- 受入れ: 1) PlanからSprintへ自然に遷移 2) ReviewでPlanとの差分が見える 3) 迷いが減る。
