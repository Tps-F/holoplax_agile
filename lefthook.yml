pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{js,ts,jsx,tsx,json}"
      run: npx biome check --staged --no-errors-on-unmatched --write
      stage_fixed: true

    - name: typecheck
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit
